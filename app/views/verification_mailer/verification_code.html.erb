<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=utf-8" http-equiv="Content-Type">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f5f5f5;
      padding: 20px;
      margin: 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 30px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }
    .content {
      padding: 40px 30px;
    }
    .code-box {
      background: #f8f9fa;
      border: 2px dashed #dee2e6;
      padding: 25px;
      text-align: center;
      font-size: 36px;
      font-weight: bold;
      letter-spacing: 8px;
      color: #667eea;
      border-radius: 8px;
      margin: 30px 0;
      font-family: "Courier New", monospace;
    }
    .info-box {
      background: #e7f3ff;
      border-left: 4px solid #2196F3;
      padding: 15px;
      margin: 20px 0;
      border-radius: 4px;
    }
    .footer {
      background: #f8f9fa;
      padding: 20px 30px;
      text-align: center;
      font-size: 12px;
      color: #6c757d;
      border-top: 1px solid #dee2e6;
    }
    .warning {
      color: #dc3545;
      font-size: 14px;
      margin-top: 20px;
      padding: 15px;
      background: #fff3cd;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>🔐 邮箱验证</h1>
    </div>

    <div class="content">
      <p>您好，</p>

      <p>您正在注册 <strong>Lazy</strong> 账号，您的邮箱验证码如下：</p>

      <div class="code-box">
        <%= @code %>
      </div>

      <div class="info-box">
        <p style="margin: 5px 0;"><strong>⏰ 有效期：</strong>10分钟</p>
        <p style="margin: 5px 0;"><strong>🕐 过期时间：</strong><%= @expires_at.strftime("%Y-%m-%d %H:%M:%S") %></p>
      </div>

      <div class="warning">
        ⚠️ 如果这不是您的操作，请忽略此邮件。
      </div>
    </div>

    <div class="footer">
      <p>此邮件由系统自动发送，请勿直接回复。</p>
      <p>&copy; <%= Time.current.year %> Lazy. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
