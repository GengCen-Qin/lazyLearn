<div class="min-h-screen flex flex-col items-center justify-center bg-white p-4"
     data-controller="xiaohongshu-downloader">
  <div class="w-full max-w-2xl flex flex-col items-center">
    <!-- Logo -->
    <h1 class="text-6xl font-bold mb-8 text-blue-600">Lazy English</h1>

    <!-- Search Form -->
    <div class="w-full max-w-xl">
      <%= form_with url: "/download_xiaohongshu", method: :post, local: true,
                    data: { xiaohongshu_downloader_target: "form",
                            action: "submit->xiaohongshu-downloader#download" } do |form| %>
        <div class="flex items-center border border-gray-300 rounded-full px-4 py-3 shadow-sm hover:shadow-md focus-within:shadow-md transition-shadow">
          <%= form.text_field :url,
                              placeholder: "请输入小红书分享内容...",
                              class: "flex-grow outline-none text-lg",
                              data: { xiaohongshu_downloader_target: "input" } %>
          <%= form.button type: "submit",
                          class: "ml-3 bg-blue-500 text-white rounded-full p-2 hover:bg-blue-600 focus:outline-none cursor-pointer" do %>
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
          <% end %>
        </div>
      <% end %>
    </div>

    <p class="mt-8 text-gray-600 text-sm">
      输入小红书分享内容，系统将自动解析并下载视频
    </p>

    <!-- Result area -->
    <div data-xiaohongshu_downloader_target="result_area" class="mt-8 w-full hidden">
      <h2 class="text-xl font-semibold mb-4">下载结果</h2>
      <div data-xiaohongshu_downloader_target="result_content" class="bg-gray-100 p-4 rounded-lg">
        <!-- Result will be displayed here -->
      </div>
    </div>
  </div>
</div>
