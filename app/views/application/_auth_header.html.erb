<%# Authentication header with login/register buttons or logged-in state %>
<div class="absolute top-6 right-6 flex items-center gap-3">
  <% if authenticated? %>
    <%# Logged in state - show green checkmark and user info %>
    <div class="flex items-center gap-2 bg-green-50 dark:bg-green-900/20 px-3 py-2 rounded-full border border-green-200 dark:border-green-800">
      <svg class="h-5 w-5 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
      <span class="text-sm text-green-700 dark:text-green-300 font-medium">
        <%= Current.user.email_address.split('@').first %>
      </span>
      <%= link_to "退出", session_path,
                  data: { turbo_method: "delete", turbo_confirm: "确定要退出登录吗？" },
                  class: "text-xs text-green-600 dark:text-green-400 hover:text-green-800 dark:hover:text-green-200 underline ml-1" %>
    </div>
  <% else %>
    <%# Logged out state - show login and register buttons %>
    <%= link_to "登录",
                new_session_path,
                class: "bg-blue-500 hover:bg-blue-700 dark:bg-blue-600 dark:hover:bg-blue-800 text-white font-bold py-2 px-4 rounded-full transition duration-200 text-sm" %>

    <%= link_to "注册",
                new_registration_path,
                class: "bg-green-500 hover:bg-green-700 dark:bg-green-600 dark:hover:bg-green-800 text-white font-bold py-2 px-4 rounded-full transition duration-200 text-sm" %>
  <% end %>
</div>